# 2026-06-21.3 指令：修正 texts/v5 文档索引中的 GDD 定位

## 任务背景

上一轮已完成：

* 在 `exp/lake-town-living-surface` 上收口提交：

  * `5bb8e8b chore: preserve v5 docs and generated uid files`
* 新建并切到：

  * `docs/v5-document-index`
* 新增并提交：

  * `94fbd74 docs: add v5 document index`
* 当前工作区干净。

但 `texts/v5/README.md` 中对 `Chronicle Game Design Document.md` 的定位不准确。

当前 README 将其描述为“原 v5 GDD / 历史版本 / 旧总纲”，但实际情况是：

```text
Chronicle Game Design Document.md
```

是用户把此前的“重构版游戏设计文档 v0.6：系统架构版”改名为 v5 后保存的主 GDD。

因此它不是旧版历史文档，而是当前 v5 文件夹中的“用户保存版主 GDD”。

本次只修正文档索引，不修改任何 Godot 文件，不修改 GDD 正文。

---

## 一、前置检查

在项目根目录执行：

```bash
git status --short
git branch --show-current
```

要求：

```text
当前分支：docs/v5-document-index
工作区：干净
```

如果不满足，立即停止并汇报。

---

## 二、修改文件

只允许修改：

```text
texts/v5/README.md
```

不要修改其他文件。

---

## 三、修改内容

将 `texts/v5/README.md` 中 `Chronicle Game Design Document.md` 的定位改为如下内容：

```markdown
### Chronicle Game Design Document.md

当前 v5 主 GDD 的用户保存版。  
该文件来源于“重构版游戏设计文档：系统架构版”，用户已将版本号调整为 v5，并作为当前 v5 文件夹中的主 GDD 保存。

用途：

- 作为当前 v5 主 GDD 的阅读入口之一
- 保留完整的系统架构版设计文本
- 便于用户直接查看、编辑和继续扩写总纲
- 与 `CHRONICLE_GDD_v5.1_REBUILD.md` 共同构成当前 v5.1 重构设计依据

注意：

- 它不是旧版历史 GDD
- 不应再标记为“旧总纲”或“历史版本”
- 若与 `CHRONICLE_GDD_v5.1_REBUILD.md` 存在重复内容，后续可再决定合并、保留或改名
```

同时修改“推荐阅读顺序”部分为：

```markdown
## 2. 推荐阅读顺序

后续讨论、写规格、让 Codex 执行前，优先按以下顺序阅读：

1. `CHRONICLE_GDD_v5.1_REBUILD.md`
2. `CHRONICLE_SYSTEM_MODULES_v5.1.md`
3. `Chronicle Game Design Document.md`

说明：

- `CHRONICLE_GDD_v5.1_REBUILD.md` 是当前重构讨论后的主执行依据。
- `CHRONICLE_SYSTEM_MODULES_v5.1.md` 是当前系统拆分与开发架构依据。
- `Chronicle Game Design Document.md` 是用户保存的 v5 主 GDD 文本，后续可根据需要与 v5.1 重构文档合并或改名。
```

如果 README 中还有“旧版”“历史版本”“旧总纲”等描述 `Chronicle Game Design Document.md` 的语句，一并修正。

---

## 四、提交

修改完成后执行：

```bash
git diff -- texts/v5/README.md
git status --short
```

确认只修改：

```text
texts/v5/README.md
```

然后提交：

```bash
git add texts/v5/README.md
git commit -m "docs: correct v5 GDD index description"
```

最后执行：

```bash
git status --short
```

确认工作区干净。

---

## 五、禁止事项

本次不要：

* 修改 Godot 场景
* 修改 GDScript 脚本
* 修改 `Chronicle Game Design Document.md`
* 修改 `CHRONICLE_GDD_v5.1_REBUILD.md`
* 修改 `CHRONICLE_SYSTEM_MODULES_v5.1.md`
* 新增路线图
* 新增 UI flow 文档
* 推送分支
* 启动任何代码重构

本次只修正 `texts/v5/README.md` 中对 GDD 文件定位的描述。
