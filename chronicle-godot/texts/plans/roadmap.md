# Chronicle 开发路线图

## Milestone 0：项目清理

目标：确认当前 Demo 边界，归档旧副本与损坏代码，建立文档和架构决策体系。

状态：执行中，详见 `milestone_0_project_cleanup.md`。

## Milestone 1：world_sim_mvp

目标：建立可以脱离玩家选择自行推进的最小世界模拟层。

状态：第一阶段核心已完成，无 UI Runner 已通过两组 30 天对比验收；尚未接入当前 Demo。

范围：

- 单个镜湖种子世界
- 地区状态
- 少量势力状态
- 资源与生态变化
- 固定时间步进
- 状态变化生成传闻与线索
- 玩家行为写回世界

详见 `milestone_1_world_sim_mvp.md`。

## Milestone 2：当前 Demo 接入

目标：保留现有线索与 MicroSession 体验，将其数据来源替换为世界模拟投影。

## Milestone 3：内容扩展与验证

目标：在模拟闭环稳定后，再扩展地区、事件、角色成长、装备、生物和关系网络。

### 2026-08-18 当前进度

- G3-D 迁入吸纳保持通过：住宅容量、职业空槽、本地工作地和社会关系共同决定迁入者能否落脚。
- 已完成 G4-A 生成期组织：聚落产业、地形、人口、道路、贸易与安全需求选择组织原型；三个聚落生成两个行路同业会和一个守路队。
- 六个创始职位由真实居民担任并链接当地资源、关系、事实和 Chronicle。成员迁出会清空当前任职、留下职位离任事实，界面首屏显示组织空缺，不保留伪现任成员。
- 组织合同 `11 / 11`、组织渲染 `7 / 7`、吸纳兼容 `10 / 10`、存档专项 `12 / 12` 已通过。
- 全项目 90 个非渲染测试与 11 个真实 Vulkan 渲染测试通过，总计 `101 / 101`。
- 已完成 G4-B1 运行期补位：空缺保留一天后，组织从同聚落真实成年人中选人；候选不足时部分补位并留下人员短缺压力，无候选时不伪造成员。
- 持续迁出路径显示“任职 1 / 空缺 1”，受控路径补满两个职位。运行期合同 `9 / 9`、累计组织渲染 `9 / 9`、G3-D 吸纳兼容 `10 / 10` 通过。
- 当前全项目 91 个非渲染测试与 11 个真实 Vulkan 渲染测试通过，总计 `102 / 102`。
- 已完成 G4-B2 压力响应：共食会等量调入本地食物，行路同业会投入道路运力增加次日容量，守路队投入道路运力降低次日货运风险与消耗。三类行动只使用组织明确链接的库存。
- 种子 `81001` 的次日石渡坞至白坡坞口粮货运达到 6 份，容量加成 2，风险由 3 降为 2；缺员和封路反事实不执行伪行动。响应合同 `11 / 11`、累计组织渲染 `11 / 11` 通过。
- 当前全项目 92 个非渲染测试与 11 个真实 Vulkan 渲染测试通过，总计 `103 / 103`。
- 已完成 G4-C1 实体生命周期事务：`create` 可在同一原子事务中建立实体、初始状态和成员关系，`update` 受白名单约束，`retire` 以软退役保留事实、关系和 Chronicle 的历史引用。每项变化必须引用真实来源事实，后续 Store 失败会整体回滚。
- 生命周期合同 `12 / 12` 通过：覆盖创建、目标变更、非法字段、未知来源、资源失败回滚、重复 ID、玩家保护、软退役、界面隐藏和磁盘存档。
- 完整回归发现旧 UX 测试把随机二选一遭遇硬编码为索赔人；测试现读取遭遇导演实际选中的可执行撤退方案，并验证处理后整个独占遭遇组消失。全项目 93 个非渲染测试与 11 个真实 Vulkan 渲染测试通过，总计 `104 / 104`，耗时 `1065.8` 秒。
- 已完成 G4-C2-A 压力驱动组织生命周期：石渡坞连续两日高粮压会由当地真实成年人形成临时共食会；粮压缓解后先转向核清余项，再于连续两日恢复后软退场。三日冷却防止反复开关，后续持续缺粮会形成第二代实体，不会复活旧组织。
- 新组织复用通用压力响应，会链接本地自然食物和口粮储备并进行守恒调拨。成立、任职、目标转向、行动、退场、Chronicle、界面和存档均引用真实来源事实。
- 生命周期专项 `14 / 14`、新增 Vulkan 合同 `8 / 8` 通过；全项目 94 个非渲染测试与 12 个真实 Vulkan 渲染测试通过，总计 `106 / 106`，耗时 `753.8` 秒。
- 已完成 G4-C2-B 多信号组织生命周期：近三日迁入家庭会促使有道路产业的聚落形成临时行路同业会；相邻道路连续高风险会促使具备守望产业和 `roadside` 地形的聚落形成临时守路队。守路队真实巡守后会降低有效风险并进入收尾，组织连续无行动时会留下效能事实、转向补人补物，四日无效后软退场。
- 同种子迁入、无迁入、高风险与低风险反事实产生不同组织史，三组种子矩阵异常数为零。专项 `15 / 15`、非渲染测试 `95 / 95`、真实 Vulkan 测试 `12 / 12`，全项目覆盖 `107 / 107`。
- 全量回归还出现一条非预写组合：白坡坞守路队创始人迁出后旧组织失能，道路风险回升，石渡坞成立临时守路队并重新任命迁入者。旧组织职位仍被正确清空，新任命属于新组织事实链。
- 已完成 G5-A 长期世界健康度：种子 `81001`、`82002`、`83003` 各自主推进 30 日，共完成 2160 个小时轮次，不含测试注入。三组世界分别产生 11、10、9 次道路压力和 52、56、51 次组织回应，历史签名与压力签名均不同。
- 三组世界均完成临时守路队的成立、目标变化、退场与冷却后再形成；失败事务、持久引用、资源越界、职位引用、人口容量和冷却异常均为零。第 30 日磁盘存档能够精确恢复，并在恢复后确定性续跑一小时。
- 初次长测虽然结构完整，却出现零道路压力、零动态组织的稳定死局。现已加入由种子、日期、道路和地形共同决定的普通环境压力，坡石、积水冲毁和横风会真实改变道路风险与日运力，并触发组织巡守或货路协调。地点面板会显示受阻原因、有效运力、风险和结束日。
- 事实快照索引、只读快照复用和批量原子事务把单种子 30 日耗时从 `1789.4` 秒降至 `387.9` 秒，约提速 `4.6` 倍。最终三种子验收耗时 `1156.5` 秒。
- 新增长测后，非渲染覆盖 `96 / 96`，真实 Vulkan 渲染覆盖 `12 / 12`，全项目 `108 / 108` 通过。
- G5-A 仍未产生自然迁徙，三组聚落人口 30 日内均保持 `10 / 10 / 10`；出生、成年、衰老、死亡和职位继任尚未实现。下一阶段进入 G5-B 最小人口生命周期，并继续降低长期模拟成本。财政、工资、派系、权力竞争和组织间政治仍未实现。
- G5-B1 已完成最小人口生命周期：生成居民拥有由种子稳定决定的出生日、生命阶段、存活状态和自然寿命；每日网络结算先处理生日、成年与死亡，再处理消费、贸易、迁徙和组织响应。
- 死亡以实体软退役保留人物、关系、物品和事实历史，同时从活动快照、生产、迁徙候选、组织任职和当前人口统计中退出。任职者死亡会形成来源明确的职位空缺，现有组织补位系统在次日从存活成年人中选出继任者。
- 种子 `81001` 的 30 人群体在没有测试注入的生命周期推进中，于第 `3148` 天出现首个自然死亡；此前形成 `257` 次年龄变化和 `7` 次成年。测试注入路径验证健康归零、家庭 Chronicle、停止生产、人口减少、职位释放、次日继任和磁盘存档往返。
- 人口生命周期专项 `8 / 8`、非渲染 `97 / 97`、真实 Vulkan `12 / 12` 通过，全项目覆盖 `109 / 109`。三种子 30 日健康度长测保持通过，耗时 `1103.0` 秒；Vulkan 耗时 `113.9` 秒。
- G5-B1 仍是单向人口生命周期。出生、伴侣形成、家庭拆分与消亡、遗产转移、疾病，以及成年居民的职业匹配尚未实现。G5-B2 先让家庭结构产生新人口和新家庭，G5-B3 再让成年与失业居民进入真实劳动力分配。
- G5-B2 已完成最小家庭世代闭环。存活成年人会按稳定种子形成伴侣，迁入有容量的家庭或建立新家庭；伴侣会在住房、人口容量、年龄、已有子女和生育冷却允许时受孕，并在孕期结束后生成新的正式人物实体。
- 新生儿从父母和本地居民定义继承文化、姓名来源、气质与属性倾向，获得父母、子女和兄弟姐妹事实与关系，并以 `generation_index` 记录世代。死亡结算会把持有物转给存活伴侣或家庭中最年长的存活成员；最后一名成员死亡时家庭软退役，历史引用继续保留。
- 种子 `81001` 的 30 人初始世界经过 35 年加速代际验证后有 49 名存活居民，形成 10 对伴侣、3 个运行期新家庭、26 次受孕与 26 次出生，其中 9 名属于第二代所生的下一代，最大世代达到 2；同时出现 7 次死亡和 3 次自然遗产转移。
- 该自然样本没有出现家庭绝户，因为后代仍存活。独立测试注入让一个家庭全员死亡，验证了家庭软退役、遗产历史和引用完整性；不能把这条受控路径写成自然绝户结果。
- 家庭世代专项 `9 / 9`、非渲染 `98 / 98`、真实 Vulkan `12 / 12` 通过，全项目覆盖 `110 / 110`。完整 Vulkan 回归还发现旧战斗渲染测试没有固定随机种子，现明确使用夹具种子 `516`，避免人物数值随机变化后继续断言旧阈值。
- G5-B3 接续解决劳动力吸纳。成年和新生代人物目前会进入失业状态，还不会依据技能、属性、家庭资源、职业空槽与聚落需求进入生产；疾病、离异、收养、育儿负担、住宅建设和更细的生育风险仍未实现。
- G5-B3 已完成本地劳动力吸纳。成年居民先保留一天求职状态，再由聚落现有产业的真实岗位空槽、属性偏好、家庭职业经验和同事熟悉度决定职业；无空槽时保留失业并按月形成求职压力，不伪造岗位。
- 就业在同一事务中更新职业、工作地、生计状态、生产标签、同事关系、来源事实和家庭 Chronicle。迁入吸纳先于本地劳动力市场运行，尚未落脚的迁入家庭不会绕过住房与生计评估。
- 实际 WorldTick 测试注入验证一名居民成年、隔日就业并在下一工作周期产生真实物品。种子 `81001` 的 35 年加速代际路径形成 17 次本地就业，其中 9 名是运行期出生后成年的居民，最高就业世代为 1；同种子就业史精确复现，种子 `82002` 形成不同历史。
- 正式地点首屏现在直接显示聚落人口上限以及在业、求职、受扶养和退休人数；最近就业也进入聚落变化记录。视觉复核确认关键信息不再藏在左栏滚动区下方。
- 劳动力专项 `10 / 10`、非渲染 `99 / 99`、真实 Vulkan `12 / 12` 通过，全项目覆盖 `111 / 111`。短版非渲染耗时 `471.3` 秒，Vulkan 耗时 `102.7` 秒，三种子 30 日无人干预长测继续通过。
- G5-C1 转向聚落容量适应。岗位和住宅上限目前仍由开局生成结果静态决定；下一步让持续失业、住房压力、本地资源与组织能力共同触发住宅、设施和产业扩张，并允许资源枯竭造成岗位收缩。工资、雇主、训练、裁员、疾病与照料仍未实现。
