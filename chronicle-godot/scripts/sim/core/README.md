# Core System

## 职责

提供世界模拟层的基础上下文、注册表和共享入口。

## 管理的数据

模拟上下文、世界标识、当前行动者、当前地点、Raw 定义索引和系统注册信息。

## 输入

Raw 定义、fixture 数据、正式世界实例和测试构造数据。

## 输出

可传递给各系统的上下文对象和定义查询入口。

## 不负责什么

不负责生成行动，不负责写回事实，不负责展示 UI。

## 与其他系统的关系

其他系统可以读取 Core 提供的上下文和注册表，但具体规则应保留在各自系统中。

## 当前状态

骨架阶段，暂未实现正式逻辑。
